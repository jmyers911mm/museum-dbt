trigger:
  branches:
    include:
      - main
  paths:
    include:
      - 'terraform/**'

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: ns11mm-terraform-dev
  - name: TF_WORKING_DIR
    value: 'terraform'
  - name: ENVIRONMENT
    value: 'dev'

stages:
  - stage: Plan
    displayName: 'Terraform Plan (Dev)'
    jobs:
      - job: Plan
        steps:
          - task: TerraformInstaller@1
            inputs:
              terraformVersion: '1.5.7'

          - task: TerraformCLI@1
            displayName: 'Terraform Init'
            inputs:
              command: 'init'
              workingDirectory: '$(TF_WORKING_DIR)'
              backendType: 'azurerm'
              backendServiceArm: 'sc-ns11mm-terraform'
              backendAzureRmResourceGroupName: 'rg-ns11mm-terraform'
              backendAzureRmStorageAccountName: 'stns11mmtfstate'
              backendAzureRmContainerName: 'tfstate'
              backendAzureRmKey: 'ns11mm-data-platform-$(ENVIRONMENT).tfstate'

          - task: TerraformCLI@1
            displayName: 'Terraform Plan'
            inputs:
              command: 'plan'
              workingDirectory: '$(TF_WORKING_DIR)'
              environmentServiceName: 'sc-ns11mm-terraform'
              commandOptions: '-var-file=environments/$(ENVIRONMENT).tfvars.json -out=tfplan'

          - task: PublishPipelineArtifact@1
            inputs:
              targetPath: '$(TF_WORKING_DIR)/tfplan'
              artifact: 'tfplan-$(ENVIRONMENT)'

  - stage: Apply
    displayName: 'Terraform Apply (Dev)'
    dependsOn: Plan
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - deployment: Apply
        environment: 'ns11mm-dev'
        strategy:
          runOnce:
            deploy:
              steps:
                - task: TerraformInstaller@1
                  inputs:
                    terraformVersion: '1.5.7'

                - task: DownloadPipelineArtifact@2
                  inputs:
                    artifact: 'tfplan-$(ENVIRONMENT)'
                    path: '$(TF_WORKING_DIR)'

                - task: TerraformCLI@1
                  displayName: 'Terraform Apply'
                  inputs:
                    command: 'apply'
                    workingDirectory: '$(TF_WORKING_DIR)'
                    environmentServiceName: 'sc-ns11mm-terraform'
                    commandOptions: 'tfplan'
